# Editor Component

Rich text editor component with advanced features.

## Usage

```tsx
import { Editor } from '@/components/Editor'

function BlogEditor() {
  return (
    <Editor
      content={content}
      onChange={setContent}
      placeholder="Start writing..."
    />
  )
}
```

## Features

- Markdown support
- Image uploads
- Formatting toolbar
- Keyboard shortcuts 