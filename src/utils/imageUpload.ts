/**
 * @deprecated Use uploadService instead
 * Re-export from centralized upload service
 */
export { uploadImage as handleImageUpload, uploadVideo as handleVideoUpload } from '@/services/uploadService';